pipeline {
    agent any
    
    stages {

        stage('checking webhook') {
            steps {
                sh 'echo "webhook is successfully triggered".'
                sh 'echo "This is to test the build after git push."'
            }
        }
    }
}